# Secrets / env — keep only the template
.env
.env.*
!.env.template

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# Node / Next
node_modules/
.next/
*.tsbuildinfo

# Build output
dist/
build/

# Local data (never commit)
uploads/
chroma_data/
app.db

# Evaluation prediction cache — regenerable, one LLM call per report
*.predictions.json

# Downloaded TCGA pathology report PDFs (re-fetchable from the GDC API)
pathology_reports/
pathology_reports_scanned/
dev.db


# Misc
.DS_Store
*.log

# Internal docs / assets — keep only the public README + deploy guide
*.md
!README.md
!DEPLOY.md
*.pdf

/test_reports


.env.template
