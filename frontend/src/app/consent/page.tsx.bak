"use client";

import { useConsent } from "@/lib/consent";

export default function ConsentPage() {
  const { giveConsent } = useConsent();

  return (
    <div className="min-h-screen bg-gradient-to-b from-white to-gray-50 flex items-center justify-center p-6">
      <div className="max-w-2xl w-full bg-white rounded-2xl shadow-sm border border-gray-100 p-8">
        <div className="text-center mb-8">
          <h1 className="text-2xl font-bold text-gray-800">
            MatchMyCancer<span className="text-blue-600">.ai</span>
          </h1>
          <p className="text-gray-500 mt-2">Important - please read before continuing</p>
        </div>

        <div className="bg-amber-50 border border-amber-200 rounded-xl p-6 mb-6">
          <h2 className="font-semibold text-amber-800 mb-3 flex items-center gap-2">
            <span className="text-xl">WARNING</span>
            This is not medical advice
          </h2>
          <ul className="text-sm text-amber-700 space-y-2">
            <li>This tool provides <strong>educational information only</strong> - it does not diagnose, prescribe, or recommend treatment.</li>
            <li>Results are generated by AI from your uploaded report and public databases (FDA labels, ClinicalTrials.gov, OncoKB).</li>
            <li>AI can miss, misinterpret, or hallucinate findings. Every claim links to the source text in your report - verify it yourself.</li>
            <li><strong>Always discuss any findings with your oncologist before acting on them.</strong></li>
          </ul>
        </div>

        <div className="bg-blue-50 border border-blue-200 rounded-xl p-6 mb-6">
          <h2 className="font-semibold text-blue-800 mb-3 flex items-center gap-2">
            <span className="text-xl">LOCK</span>
            Your data
          </h2>
          <ul className="text-sm text-blue-700 space-y-2">
            <li>Your uploaded document is processed <strong>in memory only</strong> - never written to disk.</li>
            <li>No account, no tracking, no persistent storage of your report.</li>
            <li>You can delete your session and all derived results at any time.
          </ul>
        </div>

        <button
          onClick={giveConsent}
          className="w-full bg-blue-600 text-white font-medium py-3 rounded-xl hover:bg-blue-700 transition-colors text-lg"
        >
          I understand - continue to upload
        </button>

        <p className="text-center text-xs text-gray-400 mt-4">
          By continuing, you acknowledge the above and consent to AI processing of your uploaded document.
        </p>
      </div>
    </div>
  );
}
